<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FolderApp.Views.SideMenu.SideMenuMaster"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sidebar="clr-namespace:FolderApp.Views.Siderbar"
    Title="Menu"
    Visual="Material"
    mc:Ignorable="d">

    <Grid VerticalOptions="Fill">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <sidebar:HeaderView Grid.Row="0"/>
        <Label Grid.Row="1" Text="Noticias" FontSize="18" Padding="15,0" TextTransform="Uppercase" FontAttributes="Bold" TextColor="{StaticResource purpleColor}">
            <Label.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="Noticias_TappedAsync" />
            </Label.GestureRecognizers>
        </Label>
        <CollectionView
            Grid.Row="2"
            HorizontalOptions="Start"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
            SelectionChangedCommand="{Binding ItemTapped}"
            ItemsSource="{Binding Categories}"
            SelectionMode="Single"
            VerticalOptions="Start">
            <CollectionView.Header>
                <Label Text="Categorías" FontSize="18" Padding="15,0" TextTransform="Uppercase"/>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="25,7">
                        <Label FontSize="15" Text="{Binding Title}" TextColor="{StaticResource purpleColor}" TextTransform="Uppercase" FontAttributes="Bold"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <BoxView Grid.Row="3"
            HeightRequest="1"
                 HorizontalOptions="Fill"
                 Color="{StaticResource purpleColor}" VerticalOptions="Start"/>
        <CollectionView
            Grid.Row="4"
            HorizontalOptions="Start"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
            SelectionChangedCommand="{Binding ItemTapped}"
            ItemsSource="{Binding MenuItems}"
            SelectionMode="Single"
            VerticalOptions="Start">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Title}" FontSize="18" Padding="15,5" TextColor="{StaticResource purpleColor}" TextTransform="Uppercase" FontAttributes="Bold"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Label Grid.Row="5"
               Padding="15,10"
            x:Name="Logout"
            FontSize="Medium"
            HorizontalOptions="StartAndExpand"
               VerticalOptions="End"
            Text="Cerrar Sesión"
               TextTransform="Uppercase"
               FontAttributes="Bold"
            TextColor="Gray">
            <Label.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="Logout_TappedAsync" />
            </Label.GestureRecognizers>
        </Label>
    </Grid>
</ContentPage>