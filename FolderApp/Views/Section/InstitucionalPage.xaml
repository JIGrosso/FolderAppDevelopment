<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:card="clr-namespace:FolderApp.Views.ItemsView"
             mc:Ignorable="d"
             x:Class="FolderApp.Views.InstitucionalPage">

    <NavigationPage.TitleView>
        <Label Text="Institucionales"
               Style="{StaticResource TitleLabel}"/>
    </NavigationPage.TitleView>

    <ContentPage.Content>
        <StackLayout Margin="5, 10">
            <ListView x:Name="InstitucionalesListView"
                      HasUnevenRows="True"
                      ItemsSource="{Binding PostsInstitucionales}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <card:CardView />
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>